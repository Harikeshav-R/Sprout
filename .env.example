# Docker Compose Environment Overrides
# These variables are substituted directly into docker-compose.yml by Docker
# before starting containers. Useful for managing production database credentials.

POSTGRES_DB=sprout
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
