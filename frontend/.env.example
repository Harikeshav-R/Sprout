# Frontend Web Application Configuration
# This file is loaded by Vite. All variables exposed to the client
# MUST be prefixed with VITE_.

# The URL where the frontend expects the FastAPI backend to be running.
VITE_API_URL=http://localhost:8000
